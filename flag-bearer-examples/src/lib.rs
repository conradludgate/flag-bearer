//! This crate has no API of its own — it only hosts runnable examples for
//! `flag-bearer`. See the `examples/` directory, and run them with
//! `cargo run -p flag-bearer-examples --example <name>`.
